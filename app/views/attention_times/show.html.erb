<div class="container">
  <div class="card padding margin-top">
    <div class="card-header">
      <h3 class="text-uppercase">
        <%= "Franja Horaria: " + @attention_time.get_by_format %>
      </h3>
    </div>
    <div class="card-body">
      <div class="row">
        <div class="col-6"><h3 class="card-title"><%= "Id asignado: " + @attention_time.id.to_s %></h3></div>
        <div class="col-6"><h3 class="card-text"><%= "Días de trabajo asociados: "+ @attention_time.get_working_days.size.to_s %></h3></div>
      </div>
      <div class="row padding">
        <div class=col-2><%= link_to 'Editar franha horaria', edit_attention_time_path(@attention_time), class: "btn btn-primary" %></div> 
        <div class=col-2><%= button_to 'Eliminar franja horaria', @attention_time , method: :delete , class: "btn btn-danger" %></div>         
      </div>
    </div>
  </div>

  <div class="row">
    <%= link_to "Volver al menu de franjas horarias", attention_time_path, class: "padding" %>
  </div>

</div>